{% extends 'base.html.twig' %}

{% block title %}Hello RegisterController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="mt-4 btn btn-primary">{{ button_label|default('Register') }}</button>
        {{ form_end(form) }}

    </div>
{% endblock %}
